{
  "name": "grow-ai",
  "version": "1.0.0",
  "description": "A modern, responsive website for \"Grow AI\" - a company that helps entrepreneurs build and grow successful AI businesses. This website is modeled after the AI Acquisition website with similar functionality and design.",
  "main": "about-script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:silk": "esbuild silk-background.js --bundle --format=iife --outfile=silk-background-bundle.js --jsx=automatic",
    "build:gradient": "esbuild gradient-text.js --bundle --format=iife --outfile=gradient-text-bundle.js --jsx=automatic",
    "build:pillnav": "esbuild pill-nav.js --bundle --format=iife --outfile=pill-nav-bundle.js --jsx=automatic",
    "build:spotlight": "esbuild spotlight-card.js --bundle --format=iife --outfile=spotlight-card-bundle.js --jsx=automatic",
    "build:chroma": "esbuild chroma-grid.js --bundle --format=iife --outfile=chroma-grid-bundle.js --jsx=automatic --loader:.css=css"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/OfferLaunch/growaiagency.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/OfferLaunch/growaiagency/issues"
  },
  "homepage": "https://github.com/OfferLaunch/growaiagency#readme",
  "devDependencies": {
    "esbuild": "^0.27.2",
    "shadcn": "^3.6.2"
  },
  "dependencies": {
    "@react-three/fiber": "^9.5.0",
    "gsap": "^3.14.2",
    "motion": "^12.23.26",
    "react": "^19.2.3",
    "react-dom": "^19.2.3",
    "three": "^0.182.0"
  }
}
